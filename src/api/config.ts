import { config } from "../constants";

// Api version
export const api = config.url.API_URL;

// Api errors types
export const SYNTAX_ERROR = 'SyntaxError';

// Api error render helper
export const SERVER_ERROR = 'Server error';
